#loads needed libraries for code
#main code
if (exists("select.source") == FALSE) select.source <- "main"
if (select.source == "main") {
#         library(fPortfolio)
#         library(PerformanceAnalytics)
#         library(tseries)
#         library(quantmod)
       
}

#imports code
if (select.source == "ms") {
library(RSelenium)
        
}
